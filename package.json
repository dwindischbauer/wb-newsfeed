{
  "name": "wb-news",
  "version": "1.0.0",
  "private": true,
  "description": "KI-gestützte News-Distribution (Diplomarbeit HTL Leonding)",
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --recursive --parallel run dev",
    "build": "pnpm --recursive run build",
    "start": "pnpm --filter api run start"
  }
}
